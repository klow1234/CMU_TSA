<!DOCTYPE html>
<%= render '/refinery/html_tag' %>
  <!--
    Hello stranger. Why are you examining the source code for this website?
    There is nothing to find here. 

    If you really want to check out the source, head on over to
    http://github.com/yihuanz/CMU_TSA
    It is formatted nicely and I think it is much easier to read.
  -->
  <% site_bar = render('/refinery/site_bar', :head => true) -%>
  <%= render '/refinery/head' %>
  <body>
    <%= site_bar -%>
    <%= render '/refinery/ie6check' if request.env['HTTP_USER_AGENT'] =~ /MSIE/ -%>
    <div class="container"> 
      <div id="page_container" class="logo_color titlebar">
        <div class="row logo_color">
          <div class="nav_bar">
            <div class="onecol last">
              <div class="logo">
                <%= link_to(image_tag('tsa_logo.png'), "/") %>
              </div>
            </div>
            <div class="twocol last">
              <div class="logo">
                <div class="title">
                  <%= link_to 'CMU TSA', "/" %>
                </div>
              </div>
            </div>
            <div class="ninecol last">
              <header id="header">
                <%= render(:partial => "/refinery/menu", :locals => {
                   :dom_id => 'menu',
                   :css => 'menu'
                 }) %>
              </header>
          </div>
        </div>
      </div>
    </div>
      <div id="page_container">
        <section id="page">
          <% if request.fullpath.to_s.include?("/blog") || request.fullpath == "/" %>
            <!-- <div id="fullwidth">
              <ul id="fullwidth_slider" class="jcarousel-skin-pika">
                <%# image_slider.each do |image| %>
                  <li>
                    <%#= image_fu image.photo, '1920x400#' %>
                    <span><%#= raw image.caption %></span>
                  </li>
                <%# end %>
              </ul>
            </div> -->
            <section id="jms-slideshow" class="jms-slideshow">
              <div class="step" data-color="color-1" data-x="2000" data-y="1000" data-z="3000" data-rotate="-20">
                <div class="jms-content">
                  <h3>Just when I thought...</h3>
                  <p>From fairest creatures we desire increase, that thereby beauty's rose might never die</p>
                  <a class="jms-link" href="#">Read more</a>
                </div>
                <%= image_tag ("1.png") %>
              </div>
              <div class="step" data-color="color-2" data-x="1000" data-z="2000" data-rotate="20">
                <div class="jms-content">
                  <h3>Holy cannoli!</h3>
                  <p>But as the riper should by time decease, his tender heir might bear his memory</p>
                  <a class="jms-link" href="#">Read more</a>
                </div>
                <%= image_tag ("2.png") %>
              </div>
              <div class="step" data-color="color-3" data-x="2000" data-y="1500" data-z="1000" data-rotate="20">
                <div class="jms-content">
                  <h3>No time to waste</h3>
                  <p>Within thine own bud buriest thy content and, tender churl, makest waste in niggarding</p>
                  <a class="jms-link" href="#">Read more</a>
                </div>
                <%= image_tag ("3.png") %>
              </div>
              <div class="step" data-color="color-4" data-x="3000" data-y="2000">
                <div class="jms-content">
                  <h3>Supercool!</h3>
                  <p>Making a famine where abundance lies, thyself thy foe, to thy sweet self too cruel</p>
                  <a class="jms-link" href="#">Read more</a>
                </div>
                <%= image_tag ("4.png") %>
              </div>
              <div class="step" data-color="color-5" data-x="4000" data-y="1500" data-z="1000" data-rotate="-20">
                <div class="jms-content">
                  <h3>Did you know that...</h3>
                  <p>Thou that art now the world's fresh ornament and only herald to the gaudy spring</p>
                  <a class="jms-link" href="#">Read more</a>
                </div>
                <%= image_tag ("5.png") %>
              </div>
            </section>
          <% end %>
          <div class="container"> 
            <div class="row">
              <div class="twelvecol last">
                <%= yield %>
              </div>
            </div>
          </div>
        </section>
        <div class="container"> 
          <div class="row">
            <div class="twelvecol last">
              <footer>
                <%= render '/refinery/footer' -%>
              </footer>
            </div>
          </div>
        </div>
      </div>
    <%= render '/refinery/javascripts' %>
  </body>
</html>
